# Issues tracker

This document tracks the issues so far.

- Both

- CLI

- VSCODE EXTENSION
- rendering issues where thinking is just in one block instead of as it comes in to keep a stead conversation trail, similar to how the cli works
- can't view thinking of past conversations
- tools use layout is broken, everything renders as text

# Instruction

Once an issue is fixed delete from the line
