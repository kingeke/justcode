// Support details are defined once in @core/support and reused here so the site
// and the VS Code extension never drift. Re-exported under the names the site
// already uses.
export { KOFI_URL as kofiUrl, CRYPTO_WALLETS as wallets } from '@core/support';
export type { CryptoWallet } from '@core/support';
